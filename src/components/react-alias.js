import { r as ReactInstance } from './index-DV-1WFZA.js';

export default ReactInstance;
export const {
  useState,
  useEffect,
  useRef,
  useContext,
  useMemo,
  useCallback,
  createContext,
  forwardRef,
  Suspense,
  lazy,
  StrictMode,
} = ReactInstance;
