import { X as gsapInstance } from './index-DV-1WFZA.js';

export default gsapInstance;
export const gsap = gsapInstance;
export const {
  timeline,
  to,
  from,
  fromTo,
  set,
  killTweensOf,
  registerPlugin,
} = gsapInstance;
